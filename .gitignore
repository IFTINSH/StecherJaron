node_modules
.next
out
dist
.DS_Store
*.log
.env*.local
.vercel
next-env.d.ts
*.tsbuildinfo

# temp trace output + raw source uploads (not used by the app)
tmp-trace
/Design ohne Titel.*
/Herobild*
# Rohmaterial — liegt aufbereitet in public/ (studio-photos, tattoos, events, video).
# Bleibt lokal liegen, muss aber nicht in jeden Netlify-Build (~11 MB).
/Studiobilder/
/Tattoobilder/
/Eventbilder/
/Introvideo*.mp4

# graphify knowledge graph output
graphify-out
